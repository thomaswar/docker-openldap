moduleload syncprov
serverID 1
checkpoint 1024 15

overlay syncprov
syncprov-checkpoint 100 10
syncprov-sessionlog 100

syncrepl rid=101
    provider=ldap://hal0.bmb.dress-code.biz:389
    type=refreshAndPersist
    retry="60 +"
    searchbase="dc=at"
    scope=sub
    schemachecking=on
    bindmethod=simple
    binddn="cn=admin,dc=at"
    credentials=changeit

mirrormode on
